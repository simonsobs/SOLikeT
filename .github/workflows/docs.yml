name: Documentation
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: |
          pip install tox-conda
      - name: Make docs
        run: |
          tox -e docs
