version: 2

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

conda:
  environment: soliket-tests.yml

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

formats: all
